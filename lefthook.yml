
pre-commit:
  parallel: true
  commands:
    lint-and-format:
      glob: "*.{js,ts,jsx,tsx}"
      run: |
        files=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(js|ts|jsx|tsx)$')
        if [ -n "$files" ]; then
          biome check --apply --write $files
          git add $files
        fi
      root: packages/app/

pre-push:
  parallel: true
  commands:
    build:
      run: make build
    test:
      run: bun test
      root: packages/app/
